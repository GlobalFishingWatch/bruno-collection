meta {
  name: Events
  seq: 9
}

docs {
  # Events API

  Query vessel activity events: fishing, encounters, port visits, loitering, and AIS gaps.

  - Search events by vessel, type, date range, and region
  - Get event statistics and aggregations
  - Retrieve event tiles for map visualization
  - Filter by confidence levels and encounter types
}
