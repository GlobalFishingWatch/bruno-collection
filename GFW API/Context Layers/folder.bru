meta {
  name: Context Layers
  seq: 5
}

docs {
  # Context Layers API

  Geographic context layers for map visualization (EEZ, MPA, RFMO boundaries, etc.).

  - Get available context layers
  - Retrieve layer tiles for map rendering
  - Access temporal context layer data
}
